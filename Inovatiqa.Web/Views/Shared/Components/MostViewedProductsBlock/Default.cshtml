@model IList<ProductOverviewModel>

@if (Model.Count > 0)
{
    <div class="block block-recently-viewed-products well" style="background-color:#fff;">
        <div class="page-title panel-heading">
            <h1>Most viewed products</h1>
        </div>
        <div class="product-grid home-page-most-viewed-products-grid row">
            @if (Model.Count > 0)
            {
                @for (var i = 0; i < Model.Count; i++)
                {
                    var product = Model[i];
                    <div class="item-box product-grid col-xs-12 col-sm-4 col-md-3 product-align clearfix">
                        <div class="thumbnail">
                            @await Html.PartialAsync("_ProductBox", product)
                        </div>
                    </div>
                }
            }
        </div>
    </div>
}




@*@model IList<ProductOverviewModel>
@if (Model.Count > 0)
{
    <div class="block block-mostly-viewed-products well" style="margin-bottom:0px;float:left;background-color:#fff;">
        <div class="page-title panel-heading">
            <h1>Most viewed products</h1>
        </div>
        <div class="product-grid home-page-most-viewed-products-grid row">
            <ul class="list">
            @for (var i = 0; i < Model.Count; i++)
            {
                var product = Model[i];

                <div class="item-box product-grid col-xs-12 col-sm-4 col-md-3 product-align clearfix">
                    <div class="thumbnail">
                        @await Html.PartialAsync("_ProductBox", product)
                    </div>
                </div>

                <li style="height:90px" class="thumbnail" @(i == Model.Count - 1 ? @Html.Raw(" class=\"last\"") : null)>
                        @if (!string.IsNullOrEmpty(product.DefaultPictureModel.ImageUrl))
                        {
                            <div class="product-picture" style="width:80px;">
                                <a href="@Url.RouteUrl("Product", new { SeName = product.SeName })" title="@product.DefaultPictureModel.Title">
                                    <img alt="@product.DefaultPictureModel.AlternateText" src="@product.DefaultPictureModel.ImageUrl" title="@product.DefaultPictureModel.Title" />
                                </a>
                            </div>
                        }
                        <a href="@Url.RouteUrl("Product", new { SeName = product.SeName })" class="product-name">@product.Name</a>
                    </li>
            }
            </ul>
        </div>
    </div>
}*@
