@model SearchBoxModel
@using Inovatiqa.Core
@*<form asp-route="ProductSearch" method="get" id="small-search-box-form-cart" style="float:left;">*@
  <div id="small-search-box-form-cart" style="float:left;">
    @if (Model.ShowSearchBox)
    {
        <div class="form-group" style="display:inline-flex">
            <input type="text" class="search-nav form-control" id="small-searchterms-cart" autocomplete="off" placeholder="" name="term" aria-label="Search product" style="width: 150px; margin-right: 10px; max-width: 200px;"/>
            @*<input type="submit" class="btn search-box-button" value="" />*@
            <div id="quick-product-view-box"></div>
            @if (Model.SearchTermMinimumLength > 0)
            {
                @Html.Resource(@<script type="text/javascript">
                $("#small-search-box-form-cart").on("submit", function (event) {
                    if ($("#small-searchterms-cart").val() == "") {
                        alert('@Html.Raw(JavaScriptEncoder.Default.Encode("Please enter some search keyword"))');
                        $("#small-searchterms-cart").focus();
                        event.preventDefault();
                    }
                });
                </script>, "js")
            }
            @if (Model.AutoCompleteEnabled)
            {
                <text>
                    @Html.Resource(@<script type="text/javascript">
                    /*$(document).ready(function () {
                        var showLinkToResultSearch;
                        var searchText;
                        $('#small-searchterms-cart').autocomplete({
                            delay: 500,
                            minLength: @(Model.SearchTermMinimumLength.ToString()),
                            source: '@(Url.RouteUrl("ProductSearchAutoComplete"))',
                            appendTo: '.search-box',
                            select: function (event, ui) {
                                $("#small-searchterms-cart").val(ui.item.label);
                                if ($('#quick_addprod').is(':checked')) {
                                        $('#selectItem').val(ui.item.id);
                                        //var params =@((int)ShoppingCartType.ShoppingCart);
                                        //AjaxCart.addproducttocart_details('/addproducttocart/details/' + ui.item.id + '/' + params+'/0', '#product-details-form', @((int)ShoppingCartType.ShoppingCart));
                                    }
                                    else {
                                            AjaxCart.quickopenproductmodal('/checkout/LoadQuickProductView/', ui.item.id);
                                            //AjaxCart.quickopenproductmodal('/checkout/LoadQuickProductView/', $('#small-searchterms-cart').val());
                                        }
                                    },
                                    //append link to the end of list
                                    open: function(event, ui) {
                                    //display link to search page
                                    if (showLinkToResultSearch) {
                                        searchText = document.getElementById("small-searchterms-cart").value;
                                        $(".ui-autocomplete").append("<li class=\"ui-menu-item\" role=\"presentation\"><a href=\"/search?q=" + searchText + "\">View all results...</a></li>");
                                    }
                                }
                            })
                            .data("ui-autocomplete")._renderItem = function(ul, item) {
                                var t = item.label;
                                showLinkToResultSearch = item.showlinktoresultsearch;
                                //html encode
                                t = htmlEncode(t);
                                return $("<li></li>")
                                    .data("item.autocomplete", item)
                                    .append("<a>@(Model.ShowProductImagesInSearchAutoComplete ? Html.Raw("<img src='\" + item.productpictureurl + \"'>") : null)" +"<span>" + t + "</span>" + "</a>")
                                .appendTo(ul);
                        };
                    });*/
                    </script>, "js")
                </text>
            }
        </div>
    }
</div>

