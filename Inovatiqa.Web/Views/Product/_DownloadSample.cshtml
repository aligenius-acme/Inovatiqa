@model ProductDetailsModel
@if (Model.HasSampleDownload)
{
    <div class="dwonload">
        <div>
                <a class="btn btn-default m_t_30" href="@Url.RouteUrl("GetSampleDownload", new { productid = Model.Id })">Download sample</a>
        </div>
    </div>
}